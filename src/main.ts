import { createApp } from 'vue'
import App from './App.vue'

// import glassmorphism from './index'
// import glassmorphism from 'vue3-glassmorphism'

const app = createApp(App)
// app.use(glassmorphism)
app.mount('#app')
